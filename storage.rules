rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 로그인한 본인(uid 일치)만 자기 사진 읽기/쓰기 가능
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
